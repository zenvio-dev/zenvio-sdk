# Notifique .NET SDK — Exemplo de uso

Exemplo de uso do SDK Notifique para .NET (WhatsApp, SMS, Email, Messages).

Configure as variáveis de ambiente `NOTIFIQUE_API_KEY`, `NOTIFIQUE_INSTANCE_ID`, `MY_PHONE` e `MY_EMAIL` e execute:

```bash
dotnet run
```
